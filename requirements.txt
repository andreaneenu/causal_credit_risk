# causal_credit_risk/requirements.txt
# =====================================
# Install all dependencies with:
#   pip install -r requirements.txt
#
# Recommended Python version: 3.9 – 3.11
# (EconML and causal-learn have limited Python 3.12 support as of 2024)

# ── Core data science ──────────────────────────────────────────────────────────
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3

# ── Causal inference ──────────────────────────────────────────────────────────
causal-learn>=0.1.3.8      # PC Algorithm, DAG structure learning
dowhy>=0.11                # Causal identification + refutation
econml>=0.15               # DML, CausalForestDML, CATE estimation

# ── Visualization ────────────────────────────────────────────────────────────
matplotlib>=3.7
networkx>=3.1

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm>=4.65                 # Progress bars (used inside causal-learn)
joblib>=1.3                # Parallel processing (used by scikit-learn)
